right um a few things. first can you add a package dot json script that runs the linter and the tests together, call it check. second the CI workflow should use that instead of running them as two separate steps. and third um there's a bunch of files still importing from the old utils path, like the C++ interop shim and a couple of others, those should point at the new one. oh and make sure camelCase stays camelCase in the config keys, the old migration script got that wrong last time and it broke everything
